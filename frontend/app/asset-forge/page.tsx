"use client";

/**
 * Standalone Asset Forge route: /asset-forge only (pitch deck flow).
 * Same content as /mission-control/asset-forge.
 */
export { default } from "../mission-control/asset-forge/page";
